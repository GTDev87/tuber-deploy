// import assert from 'assert';
// import tuberDeploy from '../lib';
console.log("test started");
var assert = require("assert"),
  tuberDeploy = require("../lib");

describe('tuber-deploy', function () {
  it('should have unit test!', function () {
  });
});
